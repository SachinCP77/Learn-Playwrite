function statuscodes(status) {
    if (status >= 200 && status <= 300) {
        console.log("fine request");

    }
}

statuscodes(300);


const statuscodesexp = function (status) {
    if (status >= 200 && status <= 300) {
        console.log("fine request");

    }
}

statuscodesexp(300);


const arrowfnc = (status) => {
    if (status >= 200 && status <= 300) {
        console.log("fine request");

    }

}
arrowfnc(200);


const arr = (stat) => {

}

const fnc = function (stat) {

}